package safe.lang;

public class Math {
	public static def max(a:Int, b:Int) {
		if ((a < b)()) return b; 
		return a; 
	}
	
	public static def min(a:Int, b:Int) {
		if ((a < b)()) return a; 
		return b; 
	}

}