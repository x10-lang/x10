package dims;

interface public interface Force extends Measure {
    @DerivedUnit(SI.newton) var newton: double = _, N = _var N: double = _;
    @DerivedUnit(32.1740485 * Mass.lb * Length.foot / (Time.s*Time.s)) var lbf: double = _;
    @DerivedUnit(Mass.g * Length.cm / (Time.s*Time.s)) var dyn: double = _;
}
