HPC Challenge Benchmark: STREAM

Usage:
	
    a) x10c -d directoryForGeneratedFiles -BAD_PLACE_RUNTIME_CHECK=false stream.x10
	* directoryForGeneratedFiles contains the generated .class and .java files;
	* add other x10c flags if necessary 
		
    b) x10 -classpath directoryForGeneratedfiles -NUMBER_OF_LOCAL_PLACES=mmm stream.stream localSize
        * mmm is the number of places to use 
        * add other x10 flags if necessary 