<?xml version="1.0" encoding="UTF-8"?>
<project name="x10.uide" default="build.update.jar" basedir=".">
	<import file="../org.eclipse.safari.x10dt.feature/buildCommon.xml"/>
	<import file="../org.eclipse.safari.update/buildPluginCommon.xml"/>

	<target name="init" depends="properties">
		<property name="plugin.name" value="x10.uide"/>
		<property name="plugin.version" value="${x10dt.version}"/>
		<property name="plugin.jar.name" value="${plugin.name}_${plugin.version}.jar"/>
		<property name="plugin.zip.name" value="${plugin.name}_${plugin.version}.zip"/>
		<property name="plugin.src.zip" value="${plugin.name}src.zip"/>
		<property name="plugin.destination" value="${basedir}"/>
		<condition property="pluginTemp" value="${buildTempFolder}/plugins">
			<isset property="buildTempFolder"/>
		</condition>
		<property name="pluginTemp" value="${basedir}"/>
		<condition property="build.result.folder" value="${pluginTemp}/${plugin.name}">
			<isset property="buildTempFolder"/>
		</condition>
		<property name="build.result.folder" value="${basedir}"/>
		<property name="temp.folder" value="${basedir}/temp.folder"/>
	</target>

	<target name="properties" if="eclipse.running">
		<property name="build.compiler" value="org.eclipse.jdt.core.JDTCompilerAdapter"/>
	</target>

	<!-- ================================================================================= -->

	<target name="plugin.jar" depends="init" unless="${plugin.jar.name}" description="Create jar: ${plugin.jar.name}.">
		<delete dir="${temp.folder}/${plugin.jar.name}.bin"/>
		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin"/>
		<!-- compile the source code -->
		<javac destdir="${temp.folder}/${plugin.jar.name}.bin" failonerror="${javacFailOnError}" verbose="${javacVerbose}" debug="${javacDebugInfo}" includeAntRuntime="no" bootclasspath="${bootclasspath}" source="1.5" target="1.5">
			<compilerarg line="${compilerArg}"/>
			<classpath>
				<pathelement location="../lpg/lpg_${x10dt.version}.jar"/>
				<pathelement location="../org.eclipse.uide.runtime/org.eclipse.uide.runtime_${x10dt.version}.jar"/>
				<pathelement location="../polyglot2/polyglot2_2.0.0.jar"/>
				<pathelement location="../x10.runtime/x10.runtime_${x10dt.version}.jar"/>
				<pathelement location="../x10.compiler/x10.compiler_${x10dt.version}.jar"/>
				<pathelement location="../com.ibm.watson.safari.x10/com.ibm.watson.safari.x10_${x10dt.version}.jar"/>
				<pathelement path="${eclipse.build.path}"/>
			</classpath>
			<src path="src/"/>
		</javac>
		<!-- Copy necessary resources -->
		<copy todir="${temp.folder}/${plugin.name}.jar.bin" failonerror="true" overwrite="false">
			<fileset dir="src/" excludes="**/*.java,**/package.htm*"/>
		</copy>
		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin/icons"/>
		<copy todir="${temp.folder}/${plugin.jar.name}.bin/icons" failonerror="true" overwrite="false">
			<fileset dir="icons/"/>
		</copy>
		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin/cheatsheets"/>
		<copy todir="${temp.folder}/${plugin.jar.name}.bin/cheatsheets" failonerror="true" overwrite="false">
			<fileset dir="cheatsheets/"/>
		</copy>
		<copy file="plugin.xml" todir="${temp.folder}/${plugin.jar.name}.bin" failonerror="true"/>
		<mkdir dir="${build.result.folder}"/>
		<jar destfile="${build.result.folder}/${plugin.jar.name}" basedir="${temp.folder}/${plugin.jar.name}.bin"
			manifest="META-INF/MANIFEST.MF"/>
		<delete dir="${temp.folder}/${plugin.jar.name}.bin"/>
	</target>
</project>
