public class HWDistArrays {
	public static void main(String[] s) {
		final dist(:rank==1 && rect) D = dist.factory.block([1:place.MAX_PLACES*10]);
		final double[:rank==1 && rect] a = new double[D] (point p[i]) { return i*(double)i; };
		finish ateach (point p : dist.UNIQUE) {
			for (point [i] : D|here) {
				System.out.println(a[i]);
			}
		}
	}
}
