<?xml version="1.0" encoding="UTF-8"?>
<project name="lpg" default="build.update.jar" basedir=".">
	<import file="../org.eclipse.safari.runtime.feature/buildCommon.xml"/>
	<import file="../com.ibm.watson.safari.update/buildPluginCommon.xml"/>

	<target name="init" depends="properties">
		<property name="plugin.name" value="lpg"/>
		<property name="plugin.version" value="${runtime.version}"/>
		<property name="plugin.jar.name" value="${plugin.name}_${plugin.version}.jar"/>
		<property name="plugin.zip.name" value="${plugin.name}_${plugin.version}.zip"/>
		<property name="plugin.src.zip" value="${plugin.name}src.zip"/>
		<property name="plugin.destination" value="${basedir}"/>
		<condition property="pluginTemp" value="${buildTempFolder}/plugins">
			<isset property="buildTempFolder"/>
		</condition>
		<property name="pluginTemp" value="${basedir}"/>
		<condition property="build.result.folder" value="${pluginTemp}/${plugin.name}">
			<isset property="buildTempFolder"/>
		</condition>
		<property name="build.result.folder" value="${basedir}"/>
		<property name="temp.folder" value="${basedir}/temp.folder"/>
	</target>

	<target name="properties" if="eclipse.running">
		<property name="build.compiler" value="org.eclipse.jdt.core.JDTCompilerAdapter"/>
	</target>

	<!-- ================================================================================= -->

	<target name="plugin.jar" depends="init" unless="${plugin.jar.name}" description="Create jar: ${plugin.jar.name}.">
		<delete dir="${temp.folder}/${plugin.jar.name}.bin"/>
		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin"/>
		<!-- Don't rebuild from source; the jar is already there; but we need to  -->
		<!-- add to it, so explode it into the temp dir before adding to that dir -->
		<unjar src="lib/lpg.jar" dest="${temp.folder}/${plugin.jar.name}.bin"/>
<!--		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin/lib"/>
		<copy todir="${temp.folder}/${plugin.jar.name}.bin/lib" failonerror="true">
			<fileset dir="lib" includes="lpg.jar"/>
		</copy>
  -->
		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin/bin"/>
		<copy todir="${temp.folder}/${plugin.jar.name}.bin/bin" failonerror="true" overwrite="false">
			<fileset dir="bin" includes="lpg*"/>
		</copy>
		<mkdir dir="${temp.folder}/${plugin.jar.name}.bin/templates"/>
		<copy todir="${temp.folder}/${plugin.jar.name}.bin/templates" failonerror="true" overwrite="false">
			<fileset dir="templates" includes="**/*"/>
		</copy>
		<jar destfile="${build.result.folder}/${plugin.jar.name}" basedir="${temp.folder}/${plugin.jar.name}.bin"
			manifest="META-INF/MANIFEST.MF"/>
		<delete dir="${temp.folder}/${plugin.jar.name}.bin"/>
	</target>
</project>
