/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;;

/**
 * The test checks that only properties can be accessed via self.
 *
 * @author vj
 */
public class SelfFieldsMustBeProperties1_MustFailCompile extends x10Test {
	class Test(public val i: int, ) /*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * The test checks that only properties can be accessed via self.
 *
 * @author vj
 */
public class SelfFieldsMustBeProperties1_MustFailCompile extends x10Test {
	class Test(int i) {
	   public val bad: boolean; // not declared as a property.
	   public def this(var ii: int): Test = {
	     i = ii;
	     bad = true;
	   }/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * The test checks that only properties can be accessed via self.
 *
 * @author vj
 */
public class SelfFieldsMustBeProperties1_MustFailCompile extends x10Test {
	class Test(int iint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * The test checks that only properties can be accessed via self.
 *
 * @author vj
 */
public class SelfFieldsMustBeProperties1_MustFailCompile extends x10Test {
	class Test(int i) {
	   public final boolean bad; // not declared as a property.
	   public Test(int ii) {
	     i = ii;
	     bad = true;
	   }
	}
	
	public def run(): boolean = {
	   var a: Test{i==52} = (Test{i==52 && self.bad==true})) new Test(52);
	    return true;
	}
	public static def main(var args: Rail[String]): void = {
		new SelfFieldsMustBeProperties1_MustFailCompile().execute();
	}
}
