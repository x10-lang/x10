****Error: examples/Constructs/DepType/TypeElaboration/CircularLV.x10:20:42:20:42:0:-1:9: ";" inserted to complete scope
/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  
 Check that circular dependencies between classes (through local variables) are handled correctly
 during TypeElaboration.
 
 *@author vj
 *
 */

import harness.x10Test;;

 public class CircularLV(public val k: int, ) extends x10Test /*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  
 Check that circular dependencies between classes (through local variables) are handled correctly
 during TypeElaboration.
 
 *@author vj
 *
 */

import harness.x10Test;

 public class CircularLV(int k)  extends x10Test { 
  
	 public def this(var k: int): CircularLV = { this.k = k}
	
	 public def run(): boolean = { 
		 var h: CLV1{i==j} = (CLV1{i==j})) new CLV1(4,4);
		 return true;
	 }
   
  public static def main(var args: Rail[String]): void = {
	  new CircularLV(4).execute();
	}/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  
 Check that circular dependencies between classes (through local variables) are handled correctly
 during TypeElaboration.
 
 *@author vj
 *
 */

import harness.x10Test;

 public class CircularLV(int kint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  
 Check that circular dependencies between classes (through local variables) are handled correctly
 during TypeElaboration.
 
 *@author vj
 *
 */

import harness.x10Test;

 public class CircularLV(int k)  extends x10Test { 
  
	 public CircularLV(int k) { this.k = k}
	
	 public boolean  run() { 
		 CLV1(:i==j) h = (CLV1(:i==j)) new CLV1(4,4);
		 return true;
	 }
   
  public static void main(String[] args) {
	  new CircularLV(4).execute();
	}
 }
