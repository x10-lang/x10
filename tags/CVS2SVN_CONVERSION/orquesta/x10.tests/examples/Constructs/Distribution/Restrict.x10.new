/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;;

/**
 * Minimal test for distribution restriction.
 */
public class Restrict extends x10Test {

	public def run(): boolean = {
		var r: region = region.factory.region(0, 100); //(low, high)
		final val R: region = region.factory.region(new Array[region] { r, r } );
		var d: dist = R->here;
		var R2: region = (d | here).region;
		return (R.size() == R2.size());
	}

	public static def main(var args: Rail[String]): void = {
		new Restrict().execute();
	}
}
