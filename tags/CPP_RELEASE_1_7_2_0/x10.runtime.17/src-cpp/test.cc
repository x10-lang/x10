#include <x10rt17.h>

// The purpose of this file is just to include all the other headers to
// allow easy checking of syntax, etc.

// One can run "make test.o all" instead of just "make" to additionally
// compile this file.

// (Note that "make all" and "make" are equivalent.)

// vim:tabstop=4:shiftwidth=4:expandtab
