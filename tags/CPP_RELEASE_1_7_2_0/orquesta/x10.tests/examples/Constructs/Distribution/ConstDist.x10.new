/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;;


public class ConstDist extends x10Test {

public def run(): boolean = {
	
	var R: region{rank==2&&zeroBased} = [0..9, 0..9];
	var D: dist{rank==2&&onePlace==here} = Dist.makeConstant(R, here);
	var a: Array[double]{rank==2&&onePlace==here&&zeroBased} = new Array[double](Dist.makeConstant(R, here));
	var b: Array[double]{rank==2&&onePlace==here&&zeroBased} = new Array[double](Dist.makeConstant(R, here));
		
	System.out.println("results are " + a + " " + b);
	return true;
}

public static def main(var args: Rail[String]): void = {
	new ConstDist().execute();
}
}
