/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  Check that a class can have a depclause, after the property list.
 
 *@author vj,10/30/2006
 *
 */

import harness.x10Test;;

 public class ClassDepClause(public val i: int, public val j: int, , public const i == j: ClassDepClause{i == j} = i == j)ClassDepClause{i == j} extends x10Test /*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  Check that a class can have a depclause, after the property list.
 
 *@author vj,10/30/2006
 *
 */

import harness.x10Test;

 public class ClassDepClause(int i, int j : i == j )  extends x10Test { 
  public def this(val i: int, val j: int): ClassDepClause = { property(i,j);}
  public def run(): boolean = { 
	  var x: ClassDepClause{i==2 && j==3} = (ClassDepClause{i==2 && j==3})) new ClassDepClause(2,3);
      return true;
    }
    
   public static def main(var args: Rail[String]): void = {
        new ClassDepClause(1,1).execute();
    }/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  Check that a class can have a depclause, after the property list.
 
 *@author vj,10/30/2006
 *
 */

import harness.x10Test;

 public class ClassDepClause(int iint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  Check that a class can have a depclause, after the property list.
 
 *@author vj,10/30/2006
 *
 */

import harness.x10Test;

 public class ClassDepClause(int i/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  Check that a class can have a depclause, after the property list.
 
 *@author vj,10/30/2006
 *
 */

import harness.x10Test;

 public class ClassDepClause(int i, int jint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
 /**  Check that a class can have a depclause, after the property list.
 
 *@author vj,10/30/2006
 *
 */

import harness.x10Test;

 public class ClassDepClause(int i, int j : i == j )  extends x10Test { 
  public ClassDepClause(final int i, final int j) { property(i,j);}
  public boolean  run() { 
	  ClassDepClause(:i==2 && j==3) x = (ClassDepClause(:i==2 && j==3)) new ClassDepClause(2,3);
      return true;
    }
    
   public static void main(String[] args) {
        new ClassDepClause(1,1).execute();
    }
   
}
