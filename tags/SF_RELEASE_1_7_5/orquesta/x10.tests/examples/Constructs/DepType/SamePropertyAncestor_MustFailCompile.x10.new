/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(public val i: int, public val j: int, ) /*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i, int j) {
		def this(var i: int, var j: int): Test = {
			this.i=i;
			this.j=j;
		}/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int iint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i, int jint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i, int j) {
		Test(int i, int j) {
			this.i=i;
			this.j=j;
		}
	}
		
	class Test2(public val i: int, ) extends Test /*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i, int j) {
		Test(int i, int j) {
			this.i=i;
			this.j=j;
		}
	}
		
	class Test2(int i) extends Test{
		def this(var i: int): Test2 = {
			super(i,i);
			this.i=i;
		}/*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i, int j) {
		Test(int i, int j) {
			this.i=i;
			this.j=j;
		}
	}
		
	class Test2(int iint*
 *
 * (C) Copyright IBM Corporation 2006
 *
 *  This file is part of X10 Test.
 *
 */
import harness.x10Test;

/**
 * Check that a class with a property P p cannot have an ancestor 
 * with a property named p (no property name overriding) 
 *
 * @author pvarma
 */
public class SamePropertyAncestor_MustFailCompile extends x10Test {
	class Test(int i, int j) {
		Test(int i, int j) {
			this.i=i;
			this.j=j;
		}
	}
		
	class Test2(int i) extends Test{
		Test2(int i) {
			super(i,i);
			this.i=i;
		}
	}
	public def run(): boolean = {
		var a: Test2 = new Test2(1);
	   return true;
	}
	public static def main(var args: Rail[String]): void = {
		new SamePropertyAncestor_MustFailCompile().execute();
	}
}
